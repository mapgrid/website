{
    "name": "root",
    "private": true,
    "version": "1.0.0",
    "devDependencies": {
        "dotenv": "^8.2.0",
        "eslint": "^5.16.0 || ^6.7.2",
        "eslint-config-airbnb-base": "^14.0.0",
        "eslint-config-prettier": "^6.9.0",
        "eslint-plugin-import": "^2.18.2",
        "jest": "^25.1.0",
        "lerna": "^3.20.2",
        "now": "^16.7.3",
        "prettier": "^1.19.1"
    },
    "scripts": {
        "build": "lerna run build",
        "format": "prettier --write \"**/*.js\" \"**/*.json\"",
        "lint": "eslint .",
        "test": "jest && yarn run lint",
        "deploy": "lerna run deploy && now --prod --token $NOW_TOKEN"
    },
    "workspaces": [
        "packages/*"
    ]
}
